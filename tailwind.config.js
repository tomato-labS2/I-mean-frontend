/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}", // 프로젝트 구조에 맞게 이 경로를 조정하세요
    // Tailwind 클래스를 사용하는 다른 경로(예: HTML 파일)를 추가하세요
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}